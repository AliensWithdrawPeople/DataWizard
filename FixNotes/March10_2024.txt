* Не работает редактирование пользователей // Fixed apparently
* Исправил логирование: перенёс в отдельную папку, настроил rotating file handler с циклом в 15 записей и максимальным размером в 1024^2 байтов.
* Не работает редактирование компаний // Была ошибка:
```python
if logo_img_id is True:
    data['logo_id'] = logo_img_id
```
Заменил на
```python
if logo_img_id is True:
    data['logo_id'] = obj.logo_id
```
logo_img_id является bool, когда происходит обновление уже существующей картинки. 

* Переработал боковое меню. Теперь выглядит покрасивше. Также сделал анимацию плавной.